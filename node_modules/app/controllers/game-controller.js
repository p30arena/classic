var PROC = require('app/data/procedure');
var DB = require('app/data/database');
var AU = require('app/utils/auth-util');

function list_games(username, token, msg) {
    AU.auth(username, token, msg, function () {
        PROC.find_games(function (err, docs) {
            if (docs) {
                msg.send(docs);
            } else {
                msg.error(1);
            }
        });
    });
}

exports.list_games = list_games;